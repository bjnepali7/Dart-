import 'namedparameter.dart';

void main() {
  // in map the name:"bijay"

  Map introduction = {"name": "bijay", "age": 10};

  // key and value is their
  // first item is key and last value is value

  //map<string,string>(we can put var and dynamic)
  //we can also make the key same datatypes

  // for taking the  some  value in their

  print(introduction["name"]);
  print(introduction.values);
   print(introduction.keys);
      print(introduction.keys.toList());
}
