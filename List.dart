void main() {
  List a = [1, 2, 3, 4];
  List<double> b = [1.2, 3.6, 4];
  // print(a);
  // print(b);
  //b.forEach(print);
  a.forEach((num) {
    print(num);
  });

  //list in dart is a collection of items of the same type. it is an ordered collection of items that can be accessed by their index. the index starts from 0. the list can be of any type, such as int, double, String, etc. the list can be mutable or immutable
  // The list can be mutable or immutable. A mutable list can be modified after it is created, while an immutable list cannot be modified after it is created.
}
