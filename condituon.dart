void main() {
  int x = 20;
  String status = x >= 18 ? "Adult" : x<12?"child":"teen";
  //condition ? true : false is the syntax for ternary operator which is a shorthand for if else statement, it evaluates the condition and returns the value based on wheather the condition is true or false.
  
  print(status);

}
