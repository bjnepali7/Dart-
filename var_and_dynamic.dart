void main() {
  var section = 11;
  //section="ee" this will bw  error because if we dont know what data to put and we put data and  the data we put  in variable will become that data type
  section = 10; // ovveride of the data
  var roll; // this  is a dynamic way
  // if we want to constantlyu put different datatype then it is used
  roll = "e";
  roll = 10;
  dynamic b;
  roll -= 2; // this is a short hand sixtax for the  roll=roll-2;
  print(roll);
}
